<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:paddingBottom="@dimen/activity_vertical_margin"
    tools:context=".FirstActivity">

    <Button

        android:id="@+id/open_second_activity"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="open second activity"
        android:onClick="openSecondActivity"
    />

    <Button

        android:id="@+id/open_with_scheme_custom"
        android:layout_below="@id/open_second_activity"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="open_with_scheme_custom"
        android:onClick="openWithSchemeCustom"
    />

    <Button

        android:id="@+id/open_with_scheme_http"
        android:layout_below="@id/open_with_scheme_custom"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="open_with_scheme_http"
        android:onClick="openWithSchemeHttp"
        />
</RelativeLayout>
